/*
2. Operadores Aritiméticos

+ soma
- Subtração
* Multiplicação
/ Divisão
% Módulo

Unário

- Menos unário

Esses operadores trabalham com dados inteiros ou flutuantes, executando as operações
aritiméticas básicas.

O operador mádulo opera somente com numeros inteiros, dando como resultado o resto do 
diviendo dividio pelo divisor:

21/5 = o valor seria 4.2, transformando o resultado em um dado flutuante, pois ele deixa
resto ao ser dividido

21%5 = 1, pois esse não é o valor da divisão, e sim o resto do dividendo ao ser dividido
pelo divisor, pois:

20/5 = (1+1+1+1+1)/5 + (1+1+1+1+1)/5 + (1+1+1+1+1)/5 + (1+1+1+1+1)/5 + 1/5

Precedência

Operadores tem uma ordem na qual devem ser executados, onde %, * e / tem prioridade em
relação a + e -. 

Podemos mudar a ordem da precedência através do uso de parenteses, por exemplo:

n = (z + y) * x; Nesse caso, a operação em parenteses será resolvida em prioridade

x - y + 5; nesse caso, a precedência dos operadoresé idêntica, o que faz com que sua
precedência fique a par dos seus posicionamentos, onde o programa sempre executa da 
esquerda para direita, fazendo a operação (x - y) executar primeiro.

Operador menos unário

Apenas indica a troca do sinal açgébrico do valor associado. sendo basicamente um
operador que multiplica o valor por -1 (assim como na matemática):

x=-8; (x = (-1)*8)
x=-x (x = (-1)*8)
*/
